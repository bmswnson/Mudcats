---
title: Pitching
---



```{r, include=FALSE}
# Installing Libraries 
#install.packages(c("tidyr", "dplyr", "knitr", "tidyverse", "yaml"))
library("tidyr")
library("yaml")
library("dplyr")
library("knitr")
library("tidyverse")
library("tinytex")
```

```{r, include=FALSE}
# Reading Data
mudcatsRaw <- read.csv("https://raw.githubusercontent.com/bmswnson/Mudcats/main/Data/currentStats.csv")
```


```{r, include=FALSE}
# Adding Full Names
mudcatsRaw$fullName <- paste(mudcatsRaw$First, mudcatsRaw$Last, sep = " ")
```



```{r, include=FALSE}
# Subsetting Stats
pitchingSelect <- mudcatsRaw %>%
  select(fullName, Last, IP, GP, GS, W, L, SV, SVO, BS, SV, 
         H, R, ER, BB, SO, HBP, ERA, WHIP, BF)
```

# Pitiching

# Individual Stats
## Dorsey
```{r}
pitchingSelect %>%
  filter(Last == "Dorsey") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Gunkel
```{r}
pitchingSelect %>%
  filter(Last == "Gunkel") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Huntington
```{r}
pitchingSelect %>%
  filter(Last == "Huntington") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Huseby
```{r}
pitchingSelect %>%
  filter(Last == "Huseby") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Liking
```{r}
pitchingSelect %>%
  filter(Last == "Liking") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Naugs
```{r}
pitchingSelect %>%
  filter(Last == "Naugle") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Pill
```{r}
pitchingSelect %>%
  filter(Last == "Pilon") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Reed
```{r}
pitchingSelect %>%
  filter(Last == "Reed") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Big Chungus
```{r}
pitchingSelect %>%
  filter(Last == "Sayles") %>%
  select(-Last, -fullName) %>%
  kable()
```

## Schwinner
```{r}
pitchingSelect %>%
  filter(Last == "Stratton") %>%
  select(-Last, -fullName) %>%
  kable()
```

## B Weids
```{r}
pitchingSelect %>%
  filter(Last == "Weidieman") %>%
  select(-Last, -fullName) %>%
  kable()
```
